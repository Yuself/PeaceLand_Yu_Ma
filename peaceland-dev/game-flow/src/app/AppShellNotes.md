# App Shell Notes

- React / Vite / TypeScript structure
- present map and florist memory split into separate feature views
- shared graph board, nav panel, and details panel
- data moved out of raw HTML into `src/data/`
- styling centralized in `src/styles/global.css`
